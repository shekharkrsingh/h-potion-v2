import { StyleSheet, TextStyle } from 'react-native';
import { ColorTheme } from '@/theme/colors';
import { typography } from '@/theme/typography';

export const createStyles = (theme: ColorTheme) => StyleSheet.create({
    // Standardized font groupings
    // This file mostly provides a way to have base styles if needed, 
    // but most styles for Text are dynamic in the component.
});
